## 1. 后端 — 工作流数据存储与 API

- [ ] 1.1 创建 `projects/<project>/workflows/` 目录结构，定义工作流 JSON Schema（name, nodes[], status, logs, created_at, updated_at）
- [ ] 1.2 实现 `GET /api/workflows` API：列出当前工程下所有工作流
- [ ] 1.3 实现 `POST /api/workflows` API：新建工作流（校验名称唯一性、非法字符）
- [ ] 1.4 实现 `GET /api/workflows/<name>` API：获取单个工作流配置
- [ ] 1.5 实现 `PUT /api/workflows/<name>` API：保存/更新工作流节点配置
- [ ] 1.6 实现 `DELETE /api/workflows/<name>` API：删除工作流
- [ ] 1.7 实现 `POST /api/workflows/<name>/run` API：启动工作流执行（异步）
- [ ] 1.8 实现 `GET /api/workflows/<name>/status` API：查询工作流执行状态
- [ ] 1.9 实现工作流执行引擎：顺序遍历 nodes，调用对应业务逻辑（复用现有训练/推理/标注 API）

## 2. 前端 — 工程界面工作流列表

- [ ] 2.1 修改 `templates/projects.html`：工程卡片或工程详情页新增「工作流」标签页入口
- [ ] 2.2 实现工作流列表渲染：展示名称、节点数、最后修改时间、运行状态
- [ ] 2.3 实现「新建工作流」按钮和表单弹窗（输入名称，校验规则与后端一致）
- [ ] 2.4 实现工作流列表的操作按钮：编辑（跳转编辑器）、删除（确认弹窗）
- [ ] 2.5 实现空状态提示：暂无工作流时的引导界面

## 3. 前端 — 工作流编辑器页面

- [ ] 3.1 新建 `templates/workflow-editor.html`：页面框架（返回按钮、工作流名称、保存/运行按钮）
- [ ] 3.2 实现节点面板：左侧展示可用节点类型（数据集导入、AI标注、人工审核、模型训练、模型推理、结果导出）
- [ ] 3.3 实现节点列表渲染：中间主区域以卡片列表形式展示已添加的节点（含顺序序号）
- [ ] 3.4 实现节点配置表单：点击节点展开配置项（如训练节点需配置 epochs、batch、模型版本等）
- [ ] 3.5 实现节点顺序调整：上移/下移按钮调整节点执行顺序
- [ ] 3.6 实现节点删除：每个节点支持删除并确认
- [ ] 3.7 实现保存工作流：调用 PUT API 持久化当前节点配置
- [ ] 3.8 实现工作流运行：点击「运行」调用 POST /run API，并跳转或展示实时状态面板

## 4. 前端 — 工作流执行状态展示

- [ ] 4.1 在工作流编辑器中实现状态面板：实时展示各节点的 pending/running/success/failed 状态
- [ ] 4.2 实现节点日志展开：点击节点可查看执行日志和输出摘要
- [ ] 4.3 实现运行控制按钮：运行中显示「停止」，失败后显示「重试」（从失败节点开始）
- [ ] 4.4 实现工作流完成摘要：所有节点成功执行后展示结果概览

## 5. 测试与验证

- [ ] 5.1 测试新建/保存/删除工作流的完整流程
- [ ] 5.2 测试工作流编辑器中节点的增删改和顺序调整
- [ ] 5.3 测试工作流运行：验证各节点按顺序执行并正确更新状态
- [ ] 5.4 测试异常处理：节点失败后正确停止并展示错误信息
- [ ] 5.5 运行 `ruff check .` 确保后端代码风格合规
